<%- include('partials/header'); -%>
    <h1>Home</h1>
    <p><%= homeStartingContent %></p>
    <% postList.forEach(element => { %>
        <h1><%= element.title %></h1>
        <!-- <p class="ellipsis"><%= element.post %></p> -->
        <p><%= element.post.substring(0, 100) + "..." %><a href="/posts/<%= element.title %>">Read More</a></p>
    <% }) %>
<%- include('partials/footer'); -%>
